import React from "react";

const Demo = () => {
  return (
    <div>
      {/* function App() {
  const [state, setState] = useState('idle');

  return (
    <ReactiveButton
      buttonState={state}
      onClick={() => {
        setState('loading');
        setTimeout(() => {
          setState('success');
        }, 2000);
      }}
    />
  );
} */}
    </div>
  );
};

export default Demo;
